{
  "name": "ratify",
  "version": "1.0.0",
  "description": "check authenticity of products",
  "main": "index.js",
  "scripts": {
    "dev": "lite-server",
    "build": "mkdir -p dist && cp -R src/* dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "html5-qrcode": "^2.2.0",
    "lite-server": "2.6"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "@truffle/hdwallet-provider": "^1.7.0",
    "cors": "^2.8.5"
  }
}
